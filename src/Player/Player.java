package Player;

public class Player {
    /**
     Scanner sc = new Scanner(System.in);
     String Player_decision = sc.nextLine();

     if (Player_decision.equalsIgnoreCase("yes") {
     // return GameState.Continue;
     }
     else if (Player_decision.equalsIgnoreCase("no")) {
     // return GameState.End;
     }
     */
}
